extends Node

enum Gender { MALE = 0, FEMALE, NONBINARY, COUNT }


# enum Type { HUMAN = 0, MONSTER, NPC, COUNT }
# we can check type instead with
# if instance is PlayerEntity
# if instance is NpcEntity
# if instance is MonsterEntity
